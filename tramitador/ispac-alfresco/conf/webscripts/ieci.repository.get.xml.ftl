<?xml version="1.0" encoding="utf-8"?>
<space id="${folder.id}">
  <name>${folder.name}</name>
  <fullPath>${folder.displayPath}/${folder.name}</fullPath>
  <size>${folderSize}</size>
  <files>${folderFiles}</files>
</space>
