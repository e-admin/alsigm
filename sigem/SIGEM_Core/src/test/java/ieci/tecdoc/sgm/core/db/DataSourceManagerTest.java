package ieci.tecdoc.sgm.core.db;

import ieci.tecdoc.sgm.core.config.impl.spring.Config;
import junit.framework.Assert;
import junit.framework.TestCase;

public class DataSourceManagerTest extends TestCase {

	public void testGetConnectionString() {
		/* test no funcionan
		try {
			DataSourceManager.getInstance().getConnection("dataSource_SIGEM", new Config("SIGEM_spring.xml"));
		} catch (Exception e) {
			fail();
		}
		Assert.assertTrue(true);
	}

	public void testGetConnection() {
		try {
			DataSourceManager.getInstance().getConnection(new Config("SIGEM_spring.xml"));
		} catch (Exception e) {
			fail();
		}
		Assert.assertTrue(true);
		*/
	}

}
